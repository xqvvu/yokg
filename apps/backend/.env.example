# =============================================================================
# Server
# =============================================================================
PORT=10001
CORS_ALLOWED_ORIGINS=http://localhost:5173
LOCALE=zh-CN
TZ=Asia/Shanghai

# =============================================================================
# BetterAuth
# =============================================================================
# Generate with: pnpx @better-auth/cli secret
BETTER_AUTH_SECRET=c958ccc04c7b60491571d67909338097d61543d24b466b7e20a4eeb08ce5c394
BETTER_AUTH_URL=http://localhost:${PORT}

# =============================================================================
# PostgreSQL
# =============================================================================
POSTGRES_PORT=5432
POSTGRES_HOST=localhost
POSTGRES_DB=core
POSTGRES_USER=postgres
POSTGRES_PASSWORD=mypwd
DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# =============================================================================
# Apache AGE
# =============================================================================
AGE_PORT=5455
AGE_HOST=localhost
AGE_DB=graph
AGE_USER=postgres
AGE_PASSWORD=mypwd
AGE_POOL_MAX_CONNECTIONS=10
AGE_POOL_IDLE_TIMEOUT_MS=10000
AGE_POOL_MAX_LIFETIME_SECONDS=21600
AGE_URL=postgres://${AGE_USER}:${AGE_PASSWORD}@${AGE_HOST}:${AGE_PORT}/${AGE_DB}

# =============================================================================
# Redis
# =============================================================================
REDIS_PORT=6379
REDIS_HOST=localhost
REDIS_DB=0
REDIS_PASSWORD=mypwd
REDIS_URL=redis://:${REDIS_PASSWORD}@${REDIS_HOST}:${REDIS_PORT}/${REDIS_DB}

# =============================================================================
# Object Storage
# =============================================================================
# Vendor: rustfs | aws-s3 | minio | r2 | oss | cos
OBJECT_STORAGE_VENDOR=minio
OBJECT_STORAGE_ACCESS_KEY=osadmin
OBJECT_STORAGE_SECRET_KEY=mypwd
OBJECT_STORAGE_REGION=cn-east-1
OBJECT_STORAGE_FORCE_PATH_STYLE=true
OBJECT_STORAGE_VENDOR_API_PORT=9000
OBJECT_STORAGE_VENDOR_CONSOLE_PORT=9001
OBJECT_STORAGE_PUBLIC_BUCKET_NAME=graph-mind-public
OBJECT_STORAGE_PRIVATE_BUCKET_NAME=graph-mind-private
OBJECT_STORAGE_ENDPOINT=http://localhost:${OBJECT_STORAGE_VENDOR_API_PORT}
